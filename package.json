{
  "name": "glyde-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Glyde AI — type an idea, own a company. Agentic one-person-company runner.",
  "scripts": {
    "dev": "concurrently -n server,client -c gray,white \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
