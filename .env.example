# Copy this file to server/.env
# With a key set, Glyde runs live on Claude. Without one, it runs on the
# built-in deterministic mock provider (full demo works, zero cost).
ANTHROPIC_API_KEY=

# Optional overrides
# LLM_PROVIDER=claude | mock   (default: auto — claude if key present, else mock)
# PORT=5171
